export type Freshness={state:'FRESH'|'DELAYED'|'STALE'|'MARKET_CLOSED'|'UNKNOWN';age_minutes:number|null;label:string};
export type Candidate={signal_id?:string;symbol:string;radar_score:number;classification:string;price:number;rvol:number;early_momentum_score:number;timestamp:string;data_quality:number;market_regime:string;daily_trend:string;breakout_distance:number;disposition:string;};
export type PositionSizing={account_equity:number;max_risk_percent:number;allowed_risk_amount:number;suggested_position_value:number;estimated_quantity:number;advisory_only:boolean};
export type TradeTarget={price:number;return_percent:number};
export type TradePlan={symbol:string;timestamp?:string;reference_price?:number;entry_zone_low?:number;entry_zone_high?:number;breakout_trigger?:number;stop_price?:number;stop_distance_percent?:number;targets?:TradeTarget[];risk_reward?:number;status:string;explanation:string[];data_age_minutes?:number|null;stale?:boolean;research_only:boolean;position_sizing?:PositionSizing};
export type Summary={provider:string;provider_health:string;paper_mode:boolean;research_only:boolean;market_open:boolean;data_timestamp:string|null;freshness:Freshness;counts:Record<string,number>;last_scan:string|null};
export type Signal=Candidate&{signal_id:string;lifecycle:string};
export type Bar={timestamp:string;open:number;high:number;low:number;close:number;volume:number};
export type Detail={symbol:string;candidate:Candidate|null;bars:Bar[];progression:Signal[];freshness:Freshness;trade_plan:TradePlan};
